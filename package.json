{
  "name": "serverless-qr",
  "version": "0.1.0",
  "scripts": {
    "start": "yarn && yarn dev",
    "dev": "netlify dev",
    "deploy": "netlify deploy"
  },
  "dependencies": {
    "@netlify/functions": "^1.6.0",
    "qrcode": "^1.5.3",
    "svgson": "^5.3.1"
  },
  "devDependencies": {
    "netlify-cli": "^15.1.1"
  }
}
